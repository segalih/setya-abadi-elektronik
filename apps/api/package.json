{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "api",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "concurrently -n \"API,JOB\" -c \"cyan,green\" \"php artisan serve --port=8000\" \"php artisan queue:listen\"",
        "serve": "php artisan serve",
        "queue": "php artisan queue:listen"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
